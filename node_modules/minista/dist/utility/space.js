function getSpace({
  nameLength = 0,
  maxNameLength = 0,
  min = 0
}) {
  const spaceCount = maxNameLength - nameLength + min;
  const space = spaceCount > 0 ? " ".repeat(spaceCount) : "";
  return space;
}
export {
  getSpace
};
