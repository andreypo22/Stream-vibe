function getElements(parsedData, selector) {
  if (Array.isArray(parsedData)) {
    return parsedData.map((data) => data.querySelectorAll(selector)).flat();
  } else {
    return parsedData.querySelectorAll(selector);
  }
}
function hasElement(parsedData, selector) {
  if (Array.isArray(parsedData)) {
    return parsedData.map((data) => data.querySelector(selector) ? true : false).some((has) => has);
  } else {
    return parsedData.querySelector(selector) ? true : false;
  }
}
function cleanElement(el, attributes) {
  attributes.map((attribute) => el.removeAttribute(attribute));
}
export {
  cleanElement,
  getElements,
  hasElement
};
