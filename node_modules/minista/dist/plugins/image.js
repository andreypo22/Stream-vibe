import path from "node:path";
import fs from "fs-extra";
function pluginImage(config) {
  const tempImageDir = path.join(config.sub.tempDir, "images");
  return {
    name: "minista-vite-plugin:image",
    async buildStart() {
      await fs.remove(tempImageDir);
    }
  };
}
export {
  pluginImage
};
