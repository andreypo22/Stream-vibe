import path from "node:path";
import fs from "fs-extra";
function pluginIcon(config) {
  const tempIconDir = path.join(config.sub.tempDir, "icons");
  return {
    name: "minista-vite-plugin:icon",
    async buildStart() {
      await fs.remove(tempIconDir);
    }
  };
}
export {
  pluginIcon
};
