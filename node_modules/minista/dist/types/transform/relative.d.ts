import type { HTMLElement as NHTMLElement } from "node-html-parser";
import type { ResolvedConfig } from "../config/index.js";
export declare function getRelativePath({ pathname, replaceTarget, assetPath, }: {
    pathname: string;
    replaceTarget: string;
    assetPath: string;
}): string;
export declare function transformRelative({ parsedHtml, pathname, config, }: {
    parsedHtml: NHTMLElement;
    pathname: string;
    config: ResolvedConfig;
}): void;
