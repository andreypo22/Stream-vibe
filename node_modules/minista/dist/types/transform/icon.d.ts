import type { ViteDevServer } from "vite";
import type { HTMLElement as NHTMLElement } from "node-html-parser";
import type { ResolvedConfig } from "../config/index.js";
import type { CreateSprites } from "../generate/sprite.js";
export declare function getIconHref(fileName: string, iconId: string, options?: {
    add?: string;
    remove?: string;
}): string;
export declare function transformIcons({ command, parsedData, config, createSprites, server, }: {
    command: "build" | "serve";
    parsedData: NHTMLElement | NHTMLElement[];
    config: ResolvedConfig;
    createSprites?: CreateSprites;
    server?: ViteDevServer;
}): Promise<void>;
