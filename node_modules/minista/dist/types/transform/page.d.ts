import type { ResolvedGlobal } from "../server/global.js";
import type { ResolvedPages } from "../server/page.js";
export declare function transformPage({ url, resolvedGlobal, resolvedPages, headTags, startTags, endTags, }: {
    url: string;
    resolvedGlobal: ResolvedGlobal;
    resolvedPages: ResolvedPages;
    headTags?: string;
    startTags?: string;
    endTags?: string;
}): string;
