import type { ResolvedConfig } from "../config/index.js";
export declare function getLinkTag({ command, name, input, attributes, config, }: {
    command: "build" | "serve";
    name: string;
    input: string;
    attributes?: string | false;
    config: ResolvedConfig;
}): string;
export declare function getScriptTag({ command, name, input, attributes, config, }: {
    command: "build" | "serve";
    name: string;
    input: string;
    attributes?: string | false;
    config: ResolvedConfig;
}): string;
export declare function transformTags({ command, pathname, config, }: {
    command: "build" | "serve";
    pathname: string;
    config: ResolvedConfig;
}): {
    headTags: string;
    startTags: string;
    endTags: string;
};
