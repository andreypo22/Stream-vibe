import type { ResolvedConfig } from "../config/index.js";
import type { SsgPages } from "../transform/ssg.js";
export declare function transformSearch({ command, ssgPages, config, }: {
    command: "build" | "serve";
    ssgPages: SsgPages;
    config: ResolvedConfig;
}): Promise<{
    words: string[];
    hits: number[];
    pages: {
        path: string;
        title: number[];
        toc: [number, string][];
        content: number[];
    }[];
}>;
