import type { ResolvedConfig } from "../config/index.js";
import type { ResolvedGlobal } from "../server/global.js";
import type { ResolvedPages } from "../server/page.js";
export type SsgPages = {
    fileName: string;
    path: string;
    group: string;
    title: string;
    draft: boolean;
    html: string;
}[];
export declare function transformSsg({ command, resolvedGlobal, resolvedPages, config, }: {
    command: "build" | "serve";
    resolvedGlobal: ResolvedGlobal;
    resolvedPages: ResolvedPages;
    config: ResolvedConfig;
}): Promise<SsgPages>;
