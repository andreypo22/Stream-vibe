import type { ResolvedConfig } from "../config/index.js";
export type CreateSprites = {
    [srcDir: string]: string;
};
export declare function generateTempSprite({ srcDir, fileName, config, }: {
    srcDir: string;
    fileName: string;
    config: ResolvedConfig;
}): Promise<void>;
export declare function generateSprites({ createSprites, config, maxNameLength, }: {
    createSprites: CreateSprites;
    config: ResolvedConfig;
    maxNameLength?: number;
}): Promise<void>;
