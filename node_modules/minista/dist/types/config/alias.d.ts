import type { AliasOptions as ViteAliasOptions } from "vite";
export type AliasPatterns = AliasObject | AliasArray;
type AliasObject = {
    [key: string]: string;
};
type AliasArray = {
    find: string;
    replacement: string;
}[];
export type ResolvedAlias = AliasArray;
export declare function resolveAlias(configAlias: AliasPatterns, viteConfigAlias: ViteAliasOptions): Promise<ResolvedAlias>;
export {};
