import type { ResolvedConfig } from "../config/index.js";
import type { SsgPages } from "../transform/ssg.js";
export type RunSsg = {
    (config: ResolvedConfig): Promise<SsgPages>;
};
export declare function runSsg(config: ResolvedConfig): Promise<SsgPages>;
